import React from 'react'
import { Playlist } from '../typings'

export default function FeaturedPlaylist({ artist, img, title }: Playlist) {

    console.log(img)
    return (
        <div>FeaturedPlaylist</div>
    )
}
